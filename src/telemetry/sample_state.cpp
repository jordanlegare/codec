#include <codec/profiles/telemetry.hpp>

namespace codec::profiles::telemetry {

// Intentionally empty for the Task 1 RED gate. The public telemetry codec
// declarations are implemented only after the failing tests are observed.

}  // namespace codec::profiles::telemetry
